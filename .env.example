DATABASE_URL=postgres://portifolio:portifolio@127.0.0.1:5432/portifolio
# Keep the total across API instances below the Neon connection limit.
DATABASE_MAX_CONNECTIONS=3
DATABASE_IDLE_TIMEOUT_SECONDS=30
# Trust X-Forwarded-For only when the API is behind a proxy you control that
# overwrites this header. Keep false for direct/local deployments.
TRUST_PROXY=false
# Optional free key. Income data falls back to Yahoo when it is omitted.
# ALPHA_VANTAGE_API_KEY=your-key

# Required to load company logos. Vite intentionally exposes VITE_ values to
# the browser; never put a Logo.dev secret key (sk_...) here.
# VITE_LOGO_DEV_PUBLISHABLE_KEY=pk_your-publishable-key
